<%=
  render Layout::ActionButtonComponent.new new_cookbook_recipe_path(format: :turbo_stream), id: :new_button,
                                           icon_class: %w[fas fa-plus], options: { color: :success }
%>