<%=
  render Layout::ActionButtonComponent.new edit_cookbook_recipe_path(recipe, format: :turbo_stream), id: :edit_button,
                                           icon_class: %w[fas fa-pen], options: { color: :info }
%>