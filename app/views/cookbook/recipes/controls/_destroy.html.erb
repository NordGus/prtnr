<%=
  render Layout::ActionButtonComponent.new delete_cookbook_recipe_path(recipe, format: :turbo_stream), id: :destroy_button,
                                           icon_class: %w[fas fa-trash], options: { color: :danger }
%>

