<%=
  render Layout::Actions::ModalTriggerComponent.new delete_diet_menu_menu_item_url(menu_item, menu_id: date_as_id(menu_item.date), format: :turbo_stream),
                                                    icon: FontAwesome::Icon::FasComponent.new('trash'),
                                                    options: { id: "#{dom_id(menu_item)}_destroy_button", color: :danger }
%>
