<%=
  render Layout::ActionButtonComponent.new delete_diet_menu_menu_item_url(menu_item, menu_id: date_as_id(menu_item.date), format: :turbo_stream),
                                           id: "#{dom_id(menu_item)}_destroy_button", icon_class: %w[fas fa-trash],
                                           options: { color: :danger, spaced: false }
%>

