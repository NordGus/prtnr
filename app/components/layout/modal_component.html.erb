<div
  data-layout-target="modal"
  class="auxiliary-panel is-clipped is-flex is-flex-direction-column-reverse <%= @expanded ? '' : 'is-hidden' %>"
  id="modal"
>
  <div id="auxiliary_panel" class="auxiliary-panel-content has-background-white p-3" data-layout-target="modalContent">
    <%= content %>
  </div>
  <div class="auxiliary-panel-background has-background-grey" data-action="click->layout#closeModal"></div>
</div>